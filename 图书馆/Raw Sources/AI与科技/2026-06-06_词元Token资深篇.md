# 词元（Token）资深篇

## 一、信息论视角：token 的"含金量"

从本质上看，大语言模型是一个序列概率预测器。每一个 token 被预测时，模型都在做一个 V 选 1 的抉择，V 是词表大小。理想情况下，一个 token 携带的原始信息量上限大约是 log₂(V) 比特。

但这与人类语言的真实信息密度严重错位。引入一个核心度量指标：**丰富度（fertility）**——一个真实单词平均会被切成多少个 token。比如，英文单词 "fertility" 约 1.3，而相应的中文词语可能高达 3 以上。

**这意味着：**

- **编码不公平**：表达同一个意思，中文消耗的 token 数量先天多于英文。在同样长度的上下文窗口中，中文能塞进的"干货"更少，或计费更高。
- **语义密度差异**：一个罕见的多音节希腊字母术语被切成 5 个 token，模型看到的是一串无意义的碎片；而 "cat" 就是一个 token。前者极易被模型遗忘或误解。

这就是一种数据效率上的结构性偏差。模型不是在字符层面浪费计算，就是在语义密度不均上丢失精度。

【可靠度评价】fertility 等指标是衡量多语言分词质量的成熟概念，信息论解释属于基础知识，可靠度很高。

---

## 二、分词算法的博弈：BPE、WordPiece 与 Unigram

表面上这些算法都产出子词，但它们的"哲学目标"截然不同，会导致巨大的行为差异。

### BPE（贪心的最优合并）
目标是最高频的相邻对。它有一种贪心惯性：一旦在早期把 "ing" 合并出来，它会倾向于把所有 "ing" 都保留为整体，哪怕在某些语境下拆成 "i" 和 "ng" 概率上更合理。训练速度快，但容易产生不合理的合并。

### WordPiece（基于互信息的合并）
选择合并对时，依据的不是频率，而是最大化训练语料的语言模型概率。它在合并 A 和 B 时，会计算合并后 AB 的似然是否比单独 A, B 显著升高。这能有效抑制不合理的合并，但训练代价更高。

### Unigram LM（逆向淘汰的减法）
与前两者相反，Unigram 从一个巨大的种子词表开始，然后反复问自己：去掉哪个 token 对整个语料概率的损害最小？不断"剪枝"，直到词表大小达标。其产物往往更接近概率上的最优分割，且能同时保留同一个词的不同切分可能性。

【不确定性】Unigram 在多语言和噪声数据上通常鲁棒性更好，但并无理论证明它在所有场景下绝对优于 BPE。

这三种算法的选择会直接影响模型对新奇短语的泛化能力。比如 BPE 可能把 "ChatGPT" 切成 "Chat" "G" "PT"，而 Unigram 在概率计算下或许保留 "GPT" 整体。

【可靠度评价】三种算法的目标与实现是自然语言处理的公认知识，其优缺点对比来源于实践经验，个别优劣判定有不确定性。

---

## 三、抛弃词元化的两条路径：字节级与下采样

### 路径 1：直接字节建模（如 ByT5, MambaByte）

输入直接是 UTF-8 字节序列，每个字节是一个 token（词表固定 256 个）。集外词、分词偏见、语言公平性问题全部消失。但序列长度暴增 4-8 倍。在标准 Transformer 中，自注意力复杂度是 N × N 量级，N 放大 5 倍，计算量放大 25 倍。

**突破点在于模型架构本身**：Mamba 等状态空间模型（SSM）具备近线性的序列建模能力，计算随长度线性增长，使得字节级建模变得可行。目前 MambaByte 在某些任务上已能以更少的参数量对抗子词级的 Transformer。

### 路径 2：字符级下采样（如 CANINE）

先使用字符级编码，然后通过下采样层（如 strided convolutions 或哈希局部注意力），将大量字符的信息压缩到较少的固定长度向量中，再送入标准 Transformer。既保留了字符级精度，又控制了序列长度。但引入了新的复杂结构，失去了纯粹性。

**核心争议**：人类语言的知识确实大量固化在"词"和"词干"的层次。放弃分词，等于强迫模型从零开始重新"发现"这些层次，需要难以想象的数据量和计算量。目前字节级模型在同尺寸下性能仍普遍弱于顶尖子词模型。

【不确定性】这条路径是否是"终局"，在研究者之间尚无共识。

【可靠度评价】字节级模型的概念及架构实现（ByT5, CANINE, MambaByte）是已发布的成熟工作，但其能否完全取代子词模型的论断存在激烈争议。

---

## 四、多模态 token 的离散化困境

要让一个自回归语言模型生成图像或语音，就必须把连续的感官数据变成一长串离散的 token。

### 图像 token 化：dVAE 方案（DALL-E 1）

1. 把图像压缩成一个压缩特征图
2. 将连续特征图的每个位置，用码本（codebook）里的最近邻矢量替代，记下该矢量的索引——这个索引就是一个视觉 token
3. 一张图就变成了如 32×32 = 1024 个视觉 token 的序列
4. 用标准 GPT 来建模这个序列，生成图像就是预测下一个 token

**问题极其棘手**：
- **信息损失**：将连续矢量强行映射到离散码本，就像用几种固定的颜色去临摹油画，会丢失细节
- **模态鸿沟**：文本 token 代表语义概念，视觉 token 代表局部纹理。强迫同一个注意力机制去学习这两种截然不同的序列关系，模型常常顾此失彼，生成图像出现逻辑矛盾（如杯子把手长反）
- **序列长度爆炸**：高清图需要极长的 token 序列（如 1024×1024 个），生成成本极高。后续工作（如 VQ-GAN 结合感知损失）本质上都在寻找更好的"离散化压缩器"

### 语音 token 化

通过 HuBERT 等模型提取连续表示，再用 K-Means 聚类成离散的语音 token，然后送给语言模型合成声音。挑战在于：语音 token 序列极长（一秒几十个），且韵律、情感信息极易在离散化中抹平。

【可靠度评价】基于 dVAE 的图像 token 化和基于聚类+HuBERT 的语音 token 化是当前多模态大模型的主流方法，过程描述可靠度高；但"如何最优离散化"是开放问题。

---

## 五、工程暗坑：特殊 token 和标记恢复

在现实应用中，操控模型的"生与死"有时不在模型权重，而在几个特殊 token 上。

### 对话模板的骨架
聊天模型使用 `<|im_start|>`user\n...`<|im_end|>` 这类特殊 token 来划分角色和回合。如果微调时弄错了它们的顺序或数量，模型会当场崩溃，轻则角色错乱，重则无视指令。它们就是模型心中的句号和段落标记。

### "分词器愈合"问题（Token Healing）
当你使用 API 做流式输出时，模型是逐个 token 生成的。假设模型输出了 "rain"，下一个 token 可能是 "ing"，拼在一起是 "raining"。在你看到 "rain" 的那一刻，你不知道后面会跟着什么。如果中途做了一个基于已看到文本的操作（如过滤敏感词 "rain"），分词边界就会带来误判或漏洞。

### 噪声注入与对抗
故意在输入中加入零宽字符或改变一个字节，能使分词器切出完全不同的 token 序列，导致模型理解紊乱，从而攻击成功。暴露出分词器在安全性上的脆弱。

【可靠度评价】特殊 token 的控制作用和 token healing 等问题是工程实践中的成熟经验与常见陷阱，可靠度很高。

---

## 六、终极挑战：分词本身会消失吗？

目前有两种未来在搏斗：

- **改良派**：继续优化子词分词，如开发自适应分词（在推理时根据上下文动态合并 token）、跨语言词表平衡技术，并把它与模型绑定得更深
- **革命派**：彻底端到端，用字节级模型 + 高效架构（Mamba, RWKV 等）取代分词。这不仅是技术选择，更关乎语言智能的哲学——语言理解的最小单元究竟应该由人类预设，还是应该由模型完全自主学习？

【核心不确定性】字节级模型若想在万亿级 token 训练后，其内部涌现出的隐式"词"或"概念"单元，能否在表达效率上匹敌人为定义、基于统计的子词，这还没有答案。它可能是通往更通用智能的必经之路，也可能是一条计算上奢侈的弯路。

【可靠度评价】这是领域的前沿争论，两大方向均有支持者，无确定性结论。

---
*来源：知识整理，日期：2026-06-06*