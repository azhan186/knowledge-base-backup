# 孤儿草稿清理 - 2026-06-24

## 背景

主人 23:38 发起"知识库整理全跑"，扫描发现"空 source 文件"。

**bug 根因**（已修复）：
- 旧版用 `len(body)` 字符数 < 500 判定
- 中文 UTF-8 每字 3 字节，导致字符数 < 500 但字节数 700-1500 的文件被误判"空"
- 新版用 `body.encode('utf-8')` 字节数 < 500 判定

**修复后真实结果**：
- 472 个 source 文件，**仅 1 个真正空**（< 500 字节）

## 清理文件

`source-2026-06-05-股票深入篇一_股价.md`（539 B / body 263 B）

- 只有 YAML frontmatter + L1-L4 单行 + 标题 + "时间/日期/来源"占位文字
- **无任何实质内容**——是未完成草稿
- 同名完整版 `source-2026-06-05-股票深入篇一_股价涨跌.md`（1780 B）已存在并完整

## 新工具

`scripts/scan-empty-sources.py` v1.0 已创建：
- 修复字节/字符混淆 bug
- 输出"真正空 (<500 字节)"和"极简 (500-1000 字节)"两档
- 36 个"极简"文件已识别，可后续审视

## 文件位置

`/home/azhan186/open claw/scripts/scan-empty-sources.py`

按 AGENTS.md 30 天软删除 / 60 天硬删除策略。
